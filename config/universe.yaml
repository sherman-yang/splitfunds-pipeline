{
  "adapter": "scripts.adapters.tsx_directory:load_universe",
  "adapter_args": {
    "exchange": "tsx",
    "queries": ["split"],
    "require_preferred": true,
    "exclude_keywords": ["ETF"]
  },
  "funds": []
}
